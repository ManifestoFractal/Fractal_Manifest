# Por enquanto, publicamos apenas o PDF do manifesto (na raiz) e o README.
# Todo o resto (fontes .md, scripts, CHANGELOG, pasta pdf/, caches) fica fora
# do repositório. Para liberar um novo arquivo, adicione-o à lista de exceções.

# Ignora tudo por padrão
*

# Exceto:
!.gitignore
!README.md
!manifesto_v*.pdf
